@model Rental

<form method="post">
    @*<input asp-for="Id" hidden /> Burda Biz Her sınıfın ve özellğin adını uygun verdiğimiz için mapleme yapmadan .net algılıyor ancak mapleme yapmamız gerekseydi metoda gidecek özelliği bu şekilde göndermemiz gerekirdi.*@
    <div class="border p-3 mt-5">
        <div class="row pb-3">
            <h2 class="text-primary">Kiralama Sil</h2>
            <hr />
        </div>

        <div class="mb-3">
            <label asp-for="StudentId" class="p-0"></label>
            <input asp-for="StudentId" disabled class="form-control" />
        </div>
        <div class="mb-3">
            <label asp-for="BookId" class="p-0"></label>
            <select asp-for="BookId" asp-items="ViewBag.Books" disabled class="form-select">
            </select>
        </div>
        </div>
        <div>
        <button type="submit" class="btn btn-primary" style="width:250px">Sil</button>

        <a asp-controller="Rental" asp-action="Index" class="btn btn-primary" style="width:250px">Geri Dön</a>
         </div>
</form>
